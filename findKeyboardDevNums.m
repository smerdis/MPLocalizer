function devNum = findKeyboardDevNums% find the device numbers for the various keypadsdevNum.Keyboard = -1; devNum.Keypad = -1; devNum.ButtonBox = -1;devNum.TTLPulse = -1;% devices = PsychHID('Devices');% for devicecounter = 1:length(devices)%     if (strcmp(devices(devicecounter).product, 'Apple Extended USB Keyboard') == 1) && (devices(devicecounter).inputs == 270) %582J%         devNum.Keyboard = devicecounter;%     end%     if (strcmp(devices(devicecounter).product, 'USB Keyboard Hub') == 1) && (devices(devicecounter).inputs == 270) %582J%         devNum.Keypad = devicecounter;%     end%     if devices(devicecounter).vendorID == 1240 % FORP %         devNum.ButtonBox = devicecounter;%     end% end